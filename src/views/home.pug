extends base.pug
include mixin/video.pug

block content 
  div.video-grid
    each video in videos
        +video(video)
        +video(video)
        +video(video)
        +video(video)
        +video(video)
        +video(video)
        +video(video)
        +video(video)
        +video(video)
        +video(video)
        +video(video)
        +video(video)
        +video(video)
    else
        span.empty__message No videos found 