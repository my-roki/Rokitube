extends base.pug

block content 
  form(method="POST")
    h4 Change Title of video
    input(name="title", placeholder="Write title", value=video.title, required)
    input(type="submit", value="Save")  